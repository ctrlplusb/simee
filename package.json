{
  "name": "simee",
  "version": "0.0.1",
  "description": "Ultra simple symlinking of local npm packages.",
  "bin": {
    "simee": "./index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=6"
  },
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.0.1",
    "fs-extra": "^4.0.1",
    "load-json-file": "^2.0.0"
  }
}
